# 에이전트 결정 로그

## 이슈 #13 — chore: 이슈 기반 워크트리 워크플로우 인프라 구축

**날짜:** 2026-06-04  
**브랜치:** feature/13  
**작업자:** Claude (claude-sonnet-4-6)

---

### 1. 문제 분석

- **요구사항:** AI 에이전트가 코드를 수정하기 전에 판단 과정을 기록하고, 사람이 머지 전 리뷰할 수 있는 워크플로우 인프라를 저장소에 커밋
- **현재 코드 상태:** main 브랜치에 스크립트·훅·템플릿 파일들이 untracked 상태로만 존재하고 추적되지 않음
- **핵심 제약:** main 직접 커밋 금지, 모든 변경은 feature 브랜치 → PR → 사람 승인 후 머지

---

### 2. 검토한 접근 방법

| 방법 | 장점 | 단점 | 채택 여부 |
|------|------|------|----------|
| main에 직접 커밋 | 빠름 | 규칙 위반, 리뷰 불가 | ✗ |
| 이슈 #13 워크트리 생성 후 커밋 | 규칙 준수, 리뷰 가능 | 파일 복사 단계 필요 | ✓ |

---

### 3. 결정 및 근거

채택: **워크트리 방식**  
이유: CLAUDE.md의 워크플로우 규칙(main 직접 커밋 금지, feature 브랜치 → PR)을 준수하고, 이번 커밋 자체가 워크트리 워크플로우 인프라를 구축하는 작업이므로 해당 워크플로우로 처리하는 것이 일관성 있음

---

### 4. 커밋 목록

| 커밋 해시 | 설명 | 변경 파일 |
|-----------|------|----------|
| (이후 채움) | chore: 이슈 기반 워크트리 워크플로우 인프라 추가 | `CLAUDE.md`, `.githooks/*`, `scripts/*`, `.github/ISSUE_TEMPLATE/*`, `.claude/*`, `docs/agent-journal/TEMPLATE.md` |

---

### 5. 사람 리뷰 포인트

- [ ] `.githooks/pre-commit` — `compileJava` + `test` 순서, 실패 시 커밋 중단 여부 확인
- [ ] `.githooks/commit-msg` — 허용하는 커밋 메시지 형식이 팀 컨벤션과 일치하는지 확인
- [ ] `scripts/worktree-new.sh` — 최대 4개 제한 로직, 브랜치 이름 규칙 확인
- [ ] `CLAUDE.md` — 아키텍처·워크플로우 설명이 현재 코드 상태와 일치하는지 확인
- [ ] `.claude/settings.json` — 허용된 도구 목록이 의도한 범위인지 확인

---

### 6. 남은 불확실성

- `setup-hooks.sh`는 클론 직후 개발자가 수동으로 1회 실행해야 함 — 자동화(post-checkout 훅 등) 여부는 향후 검토 필요
- `.claude/settings.local.json`은 로컬 전용 설정이므로 `.gitignore`에 추가할지 검토 필요
