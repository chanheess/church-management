#!/bin/bash
set -euo pipefail

ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"

echo "▶ [pre-commit] 컴파일 체크..."
./gradlew -q compileJava || { echo "✗ 컴파일 실패. 커밋 중단."; exit 1; }

echo "▶ [pre-commit] 테스트 실행..."
./gradlew -q test || { echo "✗ 테스트 실패. 커밋 중단."; exit 1; }

echo "✓ [pre-commit] 통과"
